
<b>Work in progress here.</b>
<br>
<br>

<div id="${html_id}" class="slickgrid-wrapper"
     style="width:100%;height:400px"></div>

<script type="text/javascript">
    jQuery(function ($) {
          $('#${html_id}').slickgrid($.extend({}, {
              // more defaults can come here
          }, ${widget_options}));
    });
</script>

<!-- Keep the old table while we develop, in the bottom. -->
<hr>

<table class="table table-striped">
    <thead>
    <tr>
        <th tal:repeat="c old_columns">${c.name}</th>
    </tr>
    </thead>
    <tbody>
    <tr tal:repeat="i old_items">
        <td tal:repeat="c old_columns">
            ${i[c.field]}
        </td>
    </tr>
    </tbody>
</table>
